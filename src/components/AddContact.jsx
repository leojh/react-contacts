import React from 'react'

const AddContact = () =>
  <div>
    <a onClick={() => {
        alert('Im adding a new contact')
      }}>
      Add New Contact
    </a>
  </div>

export default AddContact
